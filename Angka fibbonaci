#include<stdio.h>
#include<conio.h>	

	int fib(int n)
	{
	if (n<2)
	{
		return 1;
	}
	else
		return fib(n - 1) + fib(n - 2);
	}	
int main()
{
	int n;
	printf("masukkan n = "); scanf("%d", &n);
	printf("\n\n");
	printf(" fibbonaci = %d", fib(n));
	getch();
	return 0;
}
